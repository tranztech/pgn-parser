# Tranz PGN specification

This directory is the language-neutral contract for Tranz PGN. The Java implementation is the initial behavioral reference, but normative behavior is defined here and exercised through `conformance/`.

The specification is pre-1.0. Changes must identify whether they affect the library version, AST schema version, or REST API version.
